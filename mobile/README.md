# Sports Buddy Mobile App

Flutter is not installed on this machine yet, so the app scaffold was not created.

## Next step

After installing Flutter, run:

```powershell
cd c:\Users\karth\Sportsbuddy\mobile
flutter create sports_buddy_app
```

Then open `sports_buddy_app` and start implementing MVP screens.
